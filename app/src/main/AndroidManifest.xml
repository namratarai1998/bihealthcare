<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.healthcare">
    
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".MainNurseActivity"></activity>
        <activity android:name=".MainPatientActivity" />
        <activity android:name=".ViewPagerActivity" />
        <activity android:name=".DoctorprofileActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
    
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PatientsatisfactionActivity" />
        <activity android:name=".DischargeActivity" />
        <activity android:name=".PatientlistActivity" />
        <activity android:name=".DoctorlistActivity" />
        <activity android:name=".AddDoctorActivity" />
        <activity android:name=".AddpatientActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".PatientVisualization" />
    </application>

</manifest>